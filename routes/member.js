const app = express()
const router = express.Router()

// here your code
// there is need to render any thing
// just give output as re.status(code).json({message:'output'})
// required routes for post request are /login , /otp , /register


module.exports = router